import React, {Component} from 'react';

const Spinner = () => {
    const styles = {
        width: '100%',
        height: '100%',
    };
    return (
        <div>
            Loading...
        </div>
    );
};

export default Spinner;